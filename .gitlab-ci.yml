image: microsoft/dotnet:latest

stages:
    - build
    - test

build:
    stage: build
    script:
        - "cd ./dpz2.Mvc/dpz2.Mvc"
        - "dotnet build"
    artifacts:
      paths:
        - bin/

test:
    stage: test
    script: 
        - "cd ./dpz2.Mvc/dpz2.Mvc"
        - "dotnet test"